[project]
name = "maintenance-goblin"
version = "0.2.0"
description = "A transparent Windows maintenance dashboard for built-in system tools."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name="Adam Johnston"}]
dependencies = ["ttkbootstrap>=1.10.1", "psutil>=5.9"]

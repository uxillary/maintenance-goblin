[project]
name = "maintenance-goblin"
version = "0.1.0"
description = "A friendly Windows maintenance utility (SFC, DISM, cleanup, defrag) with a polished UI."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name="Adam Johnston"}]
dependencies = ["ttkbootstrap>=1.10.1", "psutil>=5.9"]
